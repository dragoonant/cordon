# Secrets — never commit
.hf_token
.HF_token
.elevenlabs_key
.env
.env.*

# Build / deps
node_modules/
dist/

# Raw AI-generated art (pre-key); published copies live in public/sprites/frames and public/portraits
public/sprites/raw/
